<UserControl
    x:Class="BrowserImageFlowNode.PropertySettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BrowserImageFlowNode"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shared="clr-namespace:Nodify;assembly=Nodify.Shared"
    d:DataContext="{d:DesignInstance local:BrowserImageFlowNodeViewModel}"
    mc:Ignorable="d">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="名称："/>
            <TextBlock Text="{Binding Title}" />
            <TextBlock  Text="图片路径：" />
            <TextBox  Text="{Binding PicPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="BBBBBBBBB"/>
        </StackPanel>
    </Grid>
</UserControl>
