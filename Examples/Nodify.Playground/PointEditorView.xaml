<UserControl
    x:Class="Nodify.Playground.PointEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Nodify.Playground"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type={x:Type local:PointEditor},
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="20" />
            <ColumnDefinition MinWidth="30" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            VerticalAlignment="Center"
            Text="X:"
            TextAlignment="Center" />
        <TextBox Grid.Column="1" Text="{Binding X, Mode=TwoWay}" />

        <TextBlock
            Grid.Row="1"
            Margin="0,5,0,0"
            VerticalAlignment="Center"
            Text="Y:"
            TextAlignment="Center" />
        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            Margin="0,5,0,0"
            Text="{Binding Y, Mode=TwoWay}" />
    </Grid>
</UserControl>
